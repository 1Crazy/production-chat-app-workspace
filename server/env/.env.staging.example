NODE_ENV=staging
PORT=3000
APP_NAME=production-chat-api-staging
# CORS 域名白名单，预发环境也必须填写明确域名，避免继承开发通配符。
CORS_ALLOWED_ORIGINS=https://staging-chat.example.com
DATABASE_URL=postgres://chat_staging:chat_staging@staging-db:5432/chat_staging
REDIS_URL=redis://staging-redis:6379/0
JWT_ACCESS_SECRET=replace-with-staging-access-secret
JWT_REFRESH_SECRET=replace-with-staging-refresh-secret
S3_ENDPOINT=https://staging-object-storage.example.com
S3_BUCKET=chat-staging
S3_ACCESS_KEY=replace-with-staging-key
S3_SECRET_KEY=replace-with-staging-secret
FCM_PROJECT_ID=
FCM_CLIENT_EMAIL=
FCM_PRIVATE_KEY=
ADMIN_HANDLES=
# 验证码投递模式。预发建议使用 webhook，提前验证真实短信/邮件链路；不要在预发暴露 debugCode。
AUTH_CODE_DELIVERY_MODE=webhook
# 是否在 /auth/request-code 响应里返回 debugCode。预发建议关闭，贴近生产安全行为。
AUTH_DEBUG_CODE_ENABLED=false
# 必填：外部验证码服务接收地址，服务会 POST identifier/purpose/code/expiresInSeconds。
AUTH_CODE_WEBHOOK_URL=https://code-provider.example.com/send
# 可选：外部验证码服务鉴权密钥。填写后后端会带 Authorization: Bearer <secret>。
AUTH_CODE_WEBHOOK_SECRET=replace-with-staging-code-webhook-secret
# 发件邮箱账号，会随 webhook payload 的 sender.email 传给外部投递服务。
AUTH_CODE_EMAIL_FROM=no-reply-staging@example.com
# 发件人展示昵称，会随 sender.nickname 传给外部投递服务。
AUTH_CODE_EMAIL_NICKNAME=Production Chat Staging
# 发件人 handle/标识，会随 sender.handle 传给外部投递服务。
AUTH_CODE_EMAIL_HANDLE=production_chat_staging
# 是否启用注册/登录/验证码/重置密码限流。预发环境建议开启，便于提前验证真实风控行为。
AUTH_RATE_LIMIT_ENABLED=true
# 鉴权限流统计窗口，单位分钟。下面各类次数限制都基于这个时间窗口计算。
AUTH_RATE_LIMIT_WINDOW_MINUTES=10
# 验证码请求：同一来源（如 IP）在窗口期内最多可请求的次数。
AUTH_REQUEST_CODE_SOURCE_LIMIT=6
# 验证码请求：同一账号标识在窗口期内最多可请求的次数。
AUTH_REQUEST_CODE_IDENTIFIER_LIMIT=3
# 注册：同一来源在窗口期内最多可尝试的次数。
AUTH_REGISTER_SOURCE_LIMIT=5
# 注册：同一账号标识在窗口期内最多可尝试的次数。
AUTH_REGISTER_IDENTIFIER_LIMIT=3
# 登录：同一来源在窗口期内最多可尝试的次数。
AUTH_LOGIN_SOURCE_LIMIT=10
# 登录：同一账号标识在窗口期内最多可尝试的次数。
AUTH_LOGIN_IDENTIFIER_LIMIT=5
# 重置密码：同一来源在窗口期内最多可尝试的次数。
AUTH_RESET_PASSWORD_SOURCE_LIMIT=5
# 重置密码：同一账号标识在窗口期内最多可尝试的次数。
AUTH_RESET_PASSWORD_IDENTIFIER_LIMIT=3
